/// <reference types="nativewind/types" />

// This makes the `className` prop type-check on React Native core components
// for consumers who opt into NativeWind. It is a no-op for the library itself
// (every component already declares `className?: string`).
