name: Publish hightide

on:
  push:
    branches:
      - main
    paths:
      - "packages/hightide/**"
      - ".github/workflows/publish-hightide.yaml"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @helpwave/hightide run init
      - run: pnpm --filter @helpwave/hightide build

      - name: Publish @helpwave/hightide
        run: pnpm --filter @helpwave/hightide publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
