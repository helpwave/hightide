{
  "name": "hightide-monorepo",
  "private": true,
  "keywords": [],
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.13.0",
      "onFail": "download"
    }
  },
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "clean": "pnpm -r run clean",
    "init": "pnpm install && pnpm -r run init",
    "build": "pnpm --filter @helpwave/hightide build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
